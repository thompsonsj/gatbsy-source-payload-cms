{
  "name": "payload-plugin-gatsby-source",
  "description": "Expose GraphQL types for Gatsby installations using a custom endpoint.",
  "version": "0.11.2",
  "license": "MIT",
  "main": "./dist/index.js",
  "files": [
    "./dist/*",
    "index.js"
  ],
  "scripts": {
    "clean": "del-cli dist",
    "build": "tsc",
    "develop": "tsc --watch",
    "test": "jest",
    "prepare": "npm run clean && npm run build"
  },
  "keywords": [
    "gatsby",
    "gatsby-plugin",
    "payload-cms"
  ],
  "devDependencies": {
    "payload": "^2",
    "typescript": "^5.3.2"
  },
  "peerDependencies": {
    "payload": "^2"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {
    "@graphql-tools/schema": "^10.0.2",
    "gatsby-source-payload-cms": "*",
    "graphql": "^16.8.1"
  }
}
